module tb;
  task automatic arr(ref bit [2:0]a[8]);
    for(int i=0;i<8;i++) begin
      a[i]=i;
    end
  endtask
  bit [2:0]a[8];
  initial begin
    arr(a);
     for(int i=0;i<8;i++) begin
       $display("a[%0d]=%0d",i,a[i]);
     end
    end
endmodule
